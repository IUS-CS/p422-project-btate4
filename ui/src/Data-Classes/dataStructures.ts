export class GenericProduct {  //not sure what the name of this should be
    id: number;
    name: string;
    img?: any; // honestly not sure how to handle images yet :)
    description?: string;
    productId: number;
    price: number;
    qty?: number;
}
